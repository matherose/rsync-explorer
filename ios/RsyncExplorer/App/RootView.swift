import SwiftUI

struct RootView: View {
    // TEMPORARY (Phase C live proof): replaced by OnboardingView/browser in Phase F.
    var body: some View {
        LiveSFTPTestView()
    }
}

#Preview {
    RootView()
}
